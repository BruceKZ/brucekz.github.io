你现在要基于“项目中已经存在的一个 demo 页面”进行重构，而不是从零新建一个普通页面。

目标是：
保留现有 demo 的基本内容方向，但彻底重构它的布局结构、滚动交互、视觉层级、组件组织方式和动画实现方式，使其变成一个真正可用的、高保真、结构先进的 scrollytelling 数据可视化页面。

技术栈与工程约束：
- Vue 3
- TypeScript
- Vite
- Tailwind CSS
- GSAP + ScrollTrigger
- 使用 Composition API
- 使用现代、清晰、可维护、可扩展的组件架构
- 可以适当抽离 composables、config、types、data mocks、animation helpers
- 不要写成单个臃肿文件
- 不要保留旧 demo 中低质量、临时拼凑式的实现

请直接以“重构现有 demo 页”为前提输出完整实现方案和代码，不要给泛泛建议，不要只给思路，不要让我自己补全关键部分。

这是一次彻底重构，重点不是微调样式，而是系统性修正以下问题：
1. 现有 demo 排版不好，需要重做信息层级、留白、栅格、全屏 section 结构、标题与图表关系。
2. 现有 demo 滚动不对，需要重做滚动驱动逻辑。
3. 现有 demo 没有实现真正的“向下滚动输入、横向叙事输出、整屏吸附切换”的体验。
4. 现有 demo 缺少高端品牌官网式的节奏感、沉浸感、控制感。
5. 现有 demo 架构不够先进，需要升级为更合理的 Vue 组件化结构。

我要的最终页面，不是普通长页面，不是普通轮播图，不是新闻专题，也不是纯动画展示页，而是：
“用户看起来是在向下滚动页面，但在关键叙事区内，页面会被 pin 住，随后随着滚轮、触控板、键盘输入，内容实际上进行横向 panel 切换或整屏状态切换，并且每一屏都具备强吸附效果，不会停在两页中间。”

请严格满足以下重构要求：

一、重构目标
- 保留现有 demo 页的主题方向，但整体视觉和交互全部升级
- 重新组织页面结构，使叙事更清晰
- 重新设计 section 之间的节奏
- 重新实现关键滚动区域
- 重新实现 panel 切换逻辑
- 重新实现整屏吸附体验
- 重新实现更现代的组件架构
- 结果必须达到“高端产品发布页 / 手机厂商官网 demo 页”的完成度

二、页面结构
请将页面重构为以下结构：

1. Hero Section
- 全屏首屏
- 强视觉中心
- 清晰的大标题、副标题、引导文案
- 有明确的向下滚动暗示
- 首屏不拥挤，强调留白和视觉聚焦
- 视觉风格偏高端科技感、深色、克制、未来感

2. Intro Section
- 简短说明这个数据故事 / 产品叙事要讲什么
- 为后续滚动叙事区做铺垫
- 信息量控制，不要做成长段说明
- 排版要比现有 demo 更干净、更有呼吸感

3. Core Storytelling Section（重点重构）
- 这是整个页面最重要的区域
- 进入该区域后，页面必须 pin 住
- 用户继续滚动时，页面不要继续正常向下，而是映射为多个 panel 的横向切换
- 该区域至少包含 5 个 full-screen panel
- 每个 panel 是一个完整叙事单元
- panel 之间必须有明确的状态切换，不允许长时间停在中间态
- panel 切换要具备强吸附感
- 可以通过 GSAP ScrollTrigger 的 pin + scrub + snap 实现
- 必要时可以增加自定义锁定/节流逻辑，以提高“整屏切换确定性”

4. Outro / Final Section
- 离开 pinned 区域后恢复正常纵向滚动
- 页面以结论、建议或 CTA 收尾
- 节奏自然收束，不要突然结束

三、核心交互要求
这是整个重构的最高优先级，必须优先保证：

1. 用户输入方式仍然是纵向滚动
- 鼠标滚轮
- 触控板
- 键盘上下键
- PageUp / PageDown

2. 关键叙事区的输出方式不是普通纵向滚动，而是横向 panel 切换
- 看起来像页面在左右推进
- 但输入仍然是纵向滚动
- 要有“滚动只是输入方式，真正被驱动的是横向叙事”的感觉

3. 必须具有强吸附
- 不能停在两个 panel 中间
- 用户停止输入后，必须自动收敛到最近 panel
- 快速滚动时也不能轻易乱跳
- 应尽量实现类似品牌官网产品页那种“滚一下，稳定进入下一屏”的体验

4. 必须具备明确的 section / panel 节奏
- 每屏是完整表达单元
- 不要让多个 panel 同时争夺注意力
- 每个 panel 的进入、停留、退出都要清楚

5. panel 切换方向以横向为主
- 默认从右向左推进
- 如设计需要，允许极少数局部反向，但整体逻辑必须统一
- 不允许无意义地来回乱切

四、内容重构要求
请基于现有 demo 内容方向，重构成一个“数据可视化滚动叙事页面”，至少包含以下 5 个 panel 类型：

panel 1：主题引入
- 用大标题、短说明、抽象视觉背景或数据纹理建立氛围
- 明确告诉用户已经进入核心叙事区

panel 2：核心指标变化
- 一个突出 KPI
- 一个折线图或柱状图
- 图表元素分阶段 reveal，不要一次性出现
- 例如：轴线 -> 数据 -> 高亮点 -> 注释

panel 3：维度/区域对比
- 条形图、分组卡片、对比图
- 必须有 1 到 2 个重点高亮项
- 高亮方式要克制，不要花哨闪烁

panel 4：趋势解释
- 时间变化趋势
- 配合解释性文案
- 数据标签、注释线、局部强调应按滚动节奏逐步出现

panel 5：总结收束
- 结论
- 2 到 3 条行动建议或洞察
- 视觉上应当有“收束”和“落点”

五、视觉重构要求
请彻底改善现有 demo 排版问题，重点修正：

1. 信息层级不清
- 重建标题、副标题、正文、数据标签、注释的层级关系

2. 留白不足或布局拥挤
- 使用更成熟的全屏布局和容器宽度控制
- 让文字与图表之间有明确的呼吸空间

3. section 节奏混乱
- 建立强节奏的 full-screen storytelling layout
- 明确每屏重点

4. 缺乏品牌官网质感
- 参考苹果、三星、手机厂商官网 demo 页的气质
- 深色背景
- 细腻渐变
- 微弱光效
- 低调网格纹理
- 分层遮罩
- 克制但高级的动效

5. 图表与文案关系松散
- 图表、标题、注释要统一纳入同一个叙事画面
- 每屏只表达一个主要结论

六、动画要求
1. 动画风格要高级、克制、统一
2. 以 transform、opacity、filter 的轻量变化为主
3. 避免廉价旋转、夸张弹跳、乱飞
4. 可使用以下动画组合：
- 标题淡入上移
- 文案延迟出现
- 图表逐步 reveal
- 背景元素轻微位移
- 高亮点依次出现
5. panel 切换时需要有明确的方向感
6. 整体动画不应影响可读性
7. 优先保证滚动控制感，再考虑花哨效果

七、架构要求
请使用尽可能先进、合理、可维护的 Vue 项目架构完成这次重构。

要求：
1. 使用 Vue 3 Composition API + TypeScript
2. 使用组件化拆分，不要把所有逻辑写在一个文件里
3. 合理拆分：
- 页面容器
- section 组件
- story panel 组件
- 数据图表组件
- composables
- 动画 hooks / helpers
- 类型定义
- mock 数据
- 配置常量
4. 动画逻辑应与 UI 结构尽量解耦
5. 滚动逻辑应尽量集中管理，不要散落在多个无序 watcher 中
6. DOM ref、GSAP timeline、ScrollTrigger 实例应清晰管理
7. onMounted 中初始化
8. onUnmounted 中彻底清理
9. 类型声明清晰，不要大量 any
10. 架构要体现现代前端工程质量，而不是 demo 拼装感

八、Tailwind 使用要求
- 允许并鼓励使用 Tailwind CSS
- 但不要堆砌 class 到不可维护
- 可以结合 clsx / class-variance-authority 或等价模式组织样式
- 要体现设计系统意识
- 间距、字号、透明度、层级、断点、容器宽度应有统一规范
- 不要写出杂乱无章的原子类堆叠
- 如有必要，可抽取基础 UI 组件或样式语义层

九、GSAP / ScrollTrigger 实现要求
必须使用 GSAP + ScrollTrigger 完成核心滚动逻辑，并明确实现：

1. pin
- pinned storytelling section 进入后固定在视口中

2. scrub
- 滚动进度可驱动动画进度

3. snap
- 自动收敛到最接近的 panel
- 保证整屏吸附感

4. horizontal translation
- 通过横向位移实现 panel 推进
- 不要做成简单轮播组件假装 scrollytelling

5. keyboard support
- 上下键、PageUp/PageDown 可驱动相邻 panel 切换
- 键盘逻辑要和滚动状态协调

6. reduced motion
- 支持 prefers-reduced-motion
- 在减少动画模式下保留基础结构和基本状态切换

十、实现质量要求
请避免以下问题：
- 不要只是修修 className
- 不要只做表层视觉优化
- 不要继续沿用错误的滚动模型
- 不要让 panel 长时间停在中间态
- 不要只做一个普通横向轮播
- 不要让 section 高度和 pin 长度计算混乱
- 不要让键盘操作失效
- 不要让 GSAP 实例泄漏
- 不要生成粗糙 demo 代码
- 不要省略关键逻辑

十一、输出格式要求
请一次性输出完整结果，内容必须包括：

1. 重构后的项目文件结构
2. 需要新增或替换的依赖安装命令
3. 每个文件的完整代码
4. 每个核心模块的职责说明
5. 滚动与吸附逻辑说明
6. 如何运行
7. 如现有 demo 中某些旧代码应删除或替换，也请明确指出

十二、重要优先级说明
如果你必须在以下几项中权衡，请按这个优先级处理：
1. 优先保证“滚动输入 -> 横向叙事输出”的正确性
2. 优先保证整屏吸附确定性，不要卡在两页中间
3. 优先保证页面节奏和信息层级
4. 优先保证组件架构先进、清晰、可维护
5. 最后再追求额外视觉装饰

十三、最终效果标准
最终结果必须让我明显感受到以下几点：
- 这不是普通滚动页面
- 向下滚动只是输入方式
- 真正被驱动的是横向叙事 panel
- 页面具有强吸附感
- 不会停在两个 panel 中间
- 排版已经明显优于旧 demo
- 架构已经不是 demo 级别，而是可继续迭代的工程化实现

请直接开始输出完整重构结果和代码，不要先解释方案，不要提可选项，不要只给片段。
